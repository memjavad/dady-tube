🎯 **What:** Removed the unused local variable `chunkIds` in `lib/providers/channel_provider.dart` (which was explicitly marked by the Dart analyzer as unused). Also formatted the file using `dart format` to ensure consistency.

💡 **Why:** Removing dead code and unneeded local variables reduces noise in the code and eliminates compiler/analyzer warnings, making the codebase cleaner and more maintainable over time.

✅ **Verification:** Verified by executing `dart test_global_health.dart` which confirmed all logic remains healthy and completely untouched, checking that no functionality was broken. Code formatting and cleanliness verified as well.

✨ **Result:** Improved code clarity, eliminated analyzer warnings without any logic side effects.
