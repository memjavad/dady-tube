## 2026-04-03 - Add Tooltips to Icon-Only Buttons
**Learning:** Icon-only buttons (like `IconButton`) lack intrinsic descriptions for screen readers, leading to poor accessibility for visually impaired users.
**Action:** Always provide a localized descriptive label via the `tooltip` property for `IconButton`s to ensure they are accessible and intuitive.
